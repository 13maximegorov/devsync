'use client';

import { TypeAnimation } from 'react-type-animation';
import { type TypeAnimationProps } from 'react-type-animation/dist/esm/components/TypeAnimation/index.types';

export const Typewriter = (props: TypeAnimationProps) => (
  <TypeAnimation {...props} />
);
